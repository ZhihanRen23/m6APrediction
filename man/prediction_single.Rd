% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Single-sample m6A prediction}
\usage{
prediction_single(ml_fit, features, positive_threshold = 0.5)
}
\arguments{
\item{ml_fit}{A trained random forest model.}

\item{features}{A named list or a one-row data.frame of input features
with column names matching the training data.}

\item{positive_threshold}{Numeric threshold for Positive classification.
Default is 0.5.}
}
\value{
A named list containing \code{predicted_m6A_prob} and \code{predicted_m6A_status}.
}
\description{
Construct a one-row feature data.frame and call
\code{\link{prediction_multiple}} internally to return the
predicted probability and classification.
}
\examples{
\dontrun{
  miniDB    <- readRDS(system.file("extdata", "miniDB.rds", package = "m6APrediction"))
  m6A_model <- read.csv(system.file("extdata", "m6A_model.csv", package = "m6APrediction"))

  # Take the first row as an example
  one_row <- m6A_model[1, , drop = FALSE]

  prediction_single(miniDB, one_row, positive_threshold = 0.5)
}
}
